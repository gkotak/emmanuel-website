{
  "name": "site-cloudcannon",
  "version": "1.0.0",
  "description": "Eleventy + local CMS data for CloudCannon experiment (Sanity untouched on main)",
  "main": "image-slot.js",
  "scripts": {
    "build": "node scripts/check-templates.js && npx --no-install eleventy",
    "dev": "npx --no-install eleventy --serve --port=8081",
    "check": "node scripts/check-templates.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/gkotak/emmanuel-website.git"
  },
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@11ty/eleventy": "^3.1.6"
  }
}
